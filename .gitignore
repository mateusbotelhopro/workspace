# Variáveis de ambiente (API keys, tokens — NUNCA commitar)
.env
.env.local
.env.*

# Arquivo gerado pelo /setup com MCPs específicos do usuário
_configurar-mcps.md

# Outputs de renderização (binários pesados)
*.png

# Ícones PWA do CRM Gestor Kelvin (necessários pro manifest.webmanifest)
!clientes/Kelvin Ulrich/crm.gestorkelvin.com.br/public/icon-192.png
!clientes/Kelvin Ulrich/crm.gestorkelvin.com.br/public/icon-512.png

# Arquivos de dados do usuário (CSVs, planilhas, etc)
dados/*.csv
dados/*.xlsx
dados/*.xls
dados/*.txt
dados/*.pdf
dados/*.json
!dados/README.md

# Arquivos de acesso dos clientes (senhas e credenciais)
**/Acessos.md

# Sistema
.DS_Store
node_modules/

# Cache local do Supabase CLI (gerado por máquina, não deve ser versionado)
**/supabase/.temp/

# Repositórios git próprios, aninhados dentro de pastas de cliente
# (já versionados/hospedados separadamente — não devem ser rastreados aqui)
clientes/joao-ferret/Elo Jurídico OS/

# Código do Leady — repositório próprio (github.com/omateusbotelho/leady)
# Os docs de planejamento (leady/*.md) continuam versionados aqui
leady/app/
